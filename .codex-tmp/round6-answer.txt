已按这轮要求处理。

主要改动：
- `D:\python\hvac-design-master-main\src\process.ts`
  - 修正结构视图状态聚合逻辑。
  - 现在只有“全部细项都完成/锁定”才会显示“已完成”。
  - 只完成一部分、其余未开始的分类或阶段，状态会显示为“进行中”，不再出现 `3/7`、`13/35` 还提示“已完成”的误判。
- `D:\python\hvac-design-master-main\src\components\dashboard\SubprojectStructureView.tsx`
  - 参照 `D:\python\hvac-design-master-main\html\暖通设计管家 (HVAC Design Master).htm` 里的紧凑头部格式，收紧了结构视图页头和页签区排版。
  - 将“阶段页签 + 分类页签”整体改成吸顶区域，页面下滚时仍可直接切换，不需要回到顶部。
  - 分类切换改成更紧凑的横向页签，完成数和最终版文件数放到第二行，减少占空。
- `D:\python\hvac-design-master-main\html`
  - 这次只把它作为版式参照，没有改动导出的 HTML 文件本身。

验证结果：
- `npm test` 通过，8 个测试文件、16 个测试全部通过。
- TypeScript 这次按改动文件做了定向检查并通过；根目录直接跑 `tsc --noEmit` 会把 `html` 目录里的 Vite 导出 `main.tsx` 一并当成源码检查，所以会报无关错误。
